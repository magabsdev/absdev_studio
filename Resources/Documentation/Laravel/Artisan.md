# Artisan

The Artisan workspace discovers commands from the selected project and supports command inputs. Output is cleared for each run and streamed to the console. Long-running interactive commands are managed separately. Commands stop with an explicit error when the selected project has no configured PHP runtime.
