# Composer

Composer commands run through the selected project PHP runtime. ABSDEV Studio can resolve a project `composer.phar`, ServBay Composer or supported package-manager installations, but execution remains tied to the project runtime. Package operations display progress and command output.
