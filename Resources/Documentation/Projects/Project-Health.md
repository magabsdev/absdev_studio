# Project Health

Project Health combines runtime, Laravel, database, filesystem, package and tooling checks. Run diagnostics after changing PHP, Composer, `.env`, database credentials or container configuration. Warnings identify optional improvements; errors identify conditions that block a workflow.
