# AI Workspace

AI features are optional and can be disabled in Settings. The AI Workspace uses the selected project as context, supports provider-specific models and can save useful conversations or responses to the Knowledge Base. Always review proposed code changes before applying them.
