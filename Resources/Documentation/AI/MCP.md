# MCP Project Intelligence

MCP tools expose local project intelligence to compatible AI clients. Each query must use the selected ABSDEV Studio project identifier rather than a stale or unrelated project definition. Use the MCP logging view to inspect requests, selected project paths and responses.
