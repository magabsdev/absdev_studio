# Git Centre

Git features operate on the selected project repository. Use them to inspect status, branches, commits and differences. Confirm the active project and branch before destructive operations. Project Timeline integration will record future Git operations.
