# Add Your First Project

Choose **Add Laravel Project…**, select the Laravel project root, and confirm the project appears in the sidebar. Open **Edit Project…** to set an alias and PHP runtime. Run **Refresh Capabilities** so package-aware tools appear. The project folder itself is never renamed when you change its alias.
