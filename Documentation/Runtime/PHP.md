# Configure PHP

Right-click a project and choose **Edit Project…**. Under **PHP Runtime**, use **Choose PHP…** to select an executable or **Detect Installed PHP** to scan supported locations. ABSDEV Studio validates the executable with `php -v` and stores the path, version and detection source on that project only.
