# Runtime Centre

Runtime configuration is project-specific. The current release stores the PHP executable and its detected version per project. Commands must use the selected project runtime rather than a global PATH assumption. Future runtime profiles extend this model to Composer, Node, npm, pnpm, Bun, Docker, Sail, ServBay and databases.
