# Deployment

Deployment tools are project-aware and should use the project runtime and environment profile. Review environment values, tests, migrations, queues and maintenance mode before deployment. Never store production secrets in documentation or the Knowledge Base.
