# Project Management

Projects are stored as references to folders on disk. You can add, remove, reorder, rename aliases and customise icons without changing the actual project folder. The selected project controls every project-aware tool, including AI and MCP queries.
